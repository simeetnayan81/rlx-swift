#!/usr/bin/env bash
# Installed via: ./scripts/install-git-hooks.sh
# Points git at this repo's .githooks/ (core.hooksPath).
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/pre-commit.sh"
